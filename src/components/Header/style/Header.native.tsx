import { StyleSheet } from 'react-native'

const style = StyleSheet.create({
    header: {
      padding: 0,
      margin: 0,
      width: '100%',
      borderWidth: 1,
    },
});

export default style
